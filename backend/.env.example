# All optional for local development.
# DATABASE_URL=sqlite:///./zoom_clone.db
# FRONTEND_URL=http://localhost:3000          # base of invite links + allowed CORS origin
# CORS_ORIGINS=http://localhost:3000          # comma-separated, overrides the defaults
# CORS_ORIGIN_REGEX=https://.*\.vercel\.app   # extra origins by pattern
